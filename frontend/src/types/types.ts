export type RoomInfo = {
    team_id: string;
    channel_id: string;
    channel_name: string;
    user_id: string;
    user_name: string;
    room_name: string;
    image_url: string;
};

export type GeneralResponse = {
    success: boolean;
};
